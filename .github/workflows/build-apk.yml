name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: 🏗 Checkout repository
      uses: actions/checkout@v3
    
    - name: 🏗 Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: 18.x
        
    - name: 🏗 Setup EAS
      uses: expo/expo-github-action@v8
      with:
        eas-version: latest
        token: ${{ secrets.EXPO_TOKEN }}
        
    - name: 📦 Install dependencies
      working-directory: ./BrauereiVerleihApp
      run: npm install --legacy-peer-deps
    
    - name: 🔧 Initialize EAS project
      working-directory: ./BrauereiVerleihApp
      run: |
        # Erstelle Projekt wenn noch nicht vorhanden
        eas project:init --non-interactive || echo "Project already exists"
    
    - name: 🚀 Build app
      working-directory: ./BrauereiVerleihApp
      run: |
        eas build --platform android --profile preview --non-interactive --no-wait
        
    - name: 📝 Build info
      run: |
        echo "✅ Build wurde gestartet!"
        echo "🔗 Prüfe deinen Expo-Account für den Download-Link"
        echo "📧 Du bekommst eine E-Mail wenn der Build fertig ist"
